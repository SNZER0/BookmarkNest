import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Chrome Bookmarks Viewer',
  description: 'A modern, responsive web application to view Chrome bookmarks in an attractive tile-based layout',
  keywords: 'bookmarks, chrome, viewer, tiles, responsive, web app',
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
